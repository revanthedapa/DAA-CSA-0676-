def maxinarr(n):
    x=sorted(n)
    print("Largest element in array is :",x[-1])
y={5,9,7,2,1}
maxi=maxinarr(y)  



def fact(n):
    if(n==0):
        return 1
    else:
        return n*fact(n-1)

f=5
factorial=fact(f)
print("factorial is:",factorial)
